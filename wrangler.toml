name = "mypainter-site"
compatibility_date = "2024-04-01"
pages_build_output_dir = "."

# D1 + R2 bindings are configured in Cloudflare Pages dashboard:
#   Settings → Functions → D1 bindings → DB → mypainter-quotes
#   Settings → Functions → R2 bindings → GALLERY → mypainter-gallery
#   Settings → Environment variables → ADMIN_PASSWORD
