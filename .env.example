# Copy to .env.local for local dev (never commit real keys)
VITE_SUPABASE_URL=https://jkampxliebnzsevvmqre.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-from-supabase-dashboard

# Cloudflare Pages secrets (not Vite — set in dashboard):
# OPENAI_API_KEY=sk-...          # receipt AI scan (/api/expenses/scan-receipt)
# VITE_SUPABASE_ANON_KEY=...     # also set as Pages secret for scan auth
